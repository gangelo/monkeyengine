# frozen_string_literal: true

require "bundler/gem_tasks"
require "rspec/core/rake_task"

require File.expand_path('lib/tasks/engine.rb', __dir__)

RSpec::Core::RakeTask.new(:spec)

task default: :spec
